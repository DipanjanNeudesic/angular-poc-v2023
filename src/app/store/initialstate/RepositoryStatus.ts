import { RepositoryStatusState } from "src/app/shared/interfaces/store/RepositoryStatus";

const initialRepositoryStatus: RepositoryStatusState={
    searchterm: "",
    totalcount:0
};

export default initialRepositoryStatus;